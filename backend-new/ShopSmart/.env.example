# ==========================================================
# =========== Ask for .env from admin over discord =========
# ==========================================================
# Copy this file to .env and fill in the values.
# NEVER commit the .env file to version control.

# --- Django Core Settings ---
SECRET_KEY="django-insecure-)t81nk()_lbwil6o$#l&fu=-y1a4tacf6uud3jv+97kucuce"
DEBUG=True
ENVIRONMENT=development  # 'development' or 'production'
ALLOWED_HOSTS=localhost,127.0.0.1

# --- Firebase Admin SDK Credentials ---
# Paste your entire Firebase service account JSON here as a single line.
# Example: '{"type":"service_account", "project_id":...}'
FIREBASE_CREDENTIALS_JSON=''

# --- Production Database (Render, Heroku, etc.) ---
# This is used when ENVIRONMENT=production
DATABASE_URL="postgresql://user:password@host:port/dbname"


# --- Development Database (Local PostgreSQL) ---
# These are used when ENVIRONMENT=development
DB_NAME=shopsmart_db
DB_USER=postgres
DB_PASSWORD=admin
DB_HOST=localhost
DB_PORT=5432


# --- Optional: Local GDAL/GEOS paths for Windows development ---
# If you are on Windows and installed OSGeo4W, you might need to specify these.
# On Linux/macOS, these are usually found automatically if gdal is installed correctly.
# GDAL_LIBRARY_PATH="C:/OSGeo4W/bin/gdal311.dll"
# GEOS_LIBRARY_PATH="C:/OSGeo4W/bin/geos_c.dll"
# PROJ_LIBRARY_PATH="C:/OSGeo4W/bin/proj.dll"